//This program will demonstrate the working of logical operator and conditional operator
#include<stdio.h>
int main()
{
    int a=10, b=2;
    if((a>=10)&&(b>=2)){// The && is the logical operator and >= is the relational operator, in this expression firstly the relational operator will be solved
        printf("hi");
    }

}